
{{ minio_log_dir }}/*.log {
    daily
    missingok
    rotate 30
    compress
    delaycompress
    notifempty
    create 0644 {{ minio_user }} {{ minio_group }}
    postrotate
        systemctl reload minio > /dev/null 2>&1 || true
    endscript
}
